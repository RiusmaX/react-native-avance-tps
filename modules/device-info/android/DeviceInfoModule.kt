// TP-02 : DeviceInfoModule.kt
// 🔲 TODO : Implémenter les 3 fonctions natives (Android)
//
// Contexte :
// - BatteryManager : context.getSystemService(Context.BATTERY_SERVICE)
//   batteryManager.getIntProperty(BatteryManager.BATTERY_PROPERTY_CAPACITY) → Int
// - Build.MODEL → String
// - PowerManager : context.getSystemService(Context.POWER_SERVICE)
//   powerManager.getThermalHeadroom() → Float (API 30+)
//
// Utiliser expo.modules.kotlin pour exposer les fonctions.

/*
package expo.modules.deviceinfo

import expo.modules.kotlin.modules.Module
import expo.modules.kotlin.modules.ModuleDefinition

class DeviceInfoModule : Module() {
  override fun definition() = ModuleDefinition {
    Name("DeviceInfo")

    // 🔲 TODO : getBatteryLevel → AsyncFunction
    // Récupérer BatteryManager → BATTERY_PROPERTY_CAPACITY

    // 🔲 TODO : getDeviceModel → Function (synchrone, JSI)
    // Retourner Build.MODEL

    // 🔲 TODO : getThermalState → AsyncFunction
    // Récupérer PowerManager → ThermalHeadroom
    // Fallback "nominal" si API < 30
  }
}
*/
